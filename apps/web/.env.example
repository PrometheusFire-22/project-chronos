# =============================================================================
# Better Auth Configuration
# =============================================================================

# Database Connection
# Better Auth tables will be created in the 'auth' schema to keep them isolated
# from your application data
DATABASE_URL="postgresql://user:password@localhost:5432/chronos?schema=auth"

# App URL (required for OAuth callbacks)
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Email Configuration (Resend)
RESEND_API_KEY="re_xxxxxxxxxxxxxxxxxxxxxxxxxxxx"
EMAIL_FROM="auth@automatonicai.com"

# Google OAuth (Optional)
GOOGLE_CLIENT_ID="your-google-client-id.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# =============================================================================
# Notes
# =============================================================================
#
# Database Schema Isolation:
# - Better Auth tables use the 'auth' schema (configured in DATABASE_URL)
# - Your application data stays in the 'public' schema
# - This prevents naming conflicts and keeps auth logic separate
#
# To create the auth schema:
# ```sql
# CREATE SCHEMA IF NOT EXISTS auth;
# GRANT ALL ON SCHEMA auth TO your_user;
# ```
#
# To generate Better Auth tables:
# ```bash
# npx @better-auth/cli generate
# ```
