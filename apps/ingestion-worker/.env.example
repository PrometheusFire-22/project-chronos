# Database Configuration
# For local testing, use your AWS RDS instance
DATABASE_URL=postgresql://username:password@your-rds-endpoint.amazonaws.com:5432/chronos

# OpenAI API (for embeddings)
OPENAI_API_KEY=sk-your-openai-api-key-here

# Modal API (for GPU processing)
# Set via: modal token new
# Stored in ~/.modal.toml automatically

# Optional: Directus CMS (if using CMS integration)
DIRECTUS_URL=https://your-directus-instance.com
DIRECTUS_TOKEN=your-directus-api-token
