# =============================================================================
# Project Chronos - Environment Variables Template
# =============================================================================
# Copy this file to create your environment-specific configuration:
#   - .env.local (local development)
#   - .env.production (production/Lightsail)
#
# NEVER commit actual .env files with secrets to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# Database (PostgreSQL + TimescaleDB)
# -----------------------------------------------------------------------------
# Local development uses Docker Compose timescaledb service
# Production uses AWS Lightsail PostgreSQL instance

DATABASE_HOST=timescaledb
DATABASE_PORT=5432
DATABASE_NAME=chronos_db
DATABASE_USER=chronos_user
DATABASE_PASSWORD=your_password_here

# Constructed DATABASE_URL (auto-generated from above)
DATABASE_URL=postgresql://${DATABASE_USER}:${DATABASE_PASSWORD}@${DATABASE_HOST}:${DATABASE_PORT}/${DATABASE_NAME}

# Alternative format (used by some services)
POSTGRES_URL=${DATABASE_URL}
POSTGRES_USER=${DATABASE_USER}
POSTGRES_PASSWORD=${DATABASE_PASSWORD}
POSTGRES_DB=${DATABASE_NAME}

# -----------------------------------------------------------------------------
# Directus CMS
# -----------------------------------------------------------------------------
DIRECTUS_DB_USER=${DATABASE_USER}
DIRECTUS_DB_PASSWORD=${DATABASE_PASSWORD}
DIRECTUS_KEY=your_random_key_here_64_chars
DIRECTUS_SECRET=your_random_secret_here_64_chars
DIRECTUS_ADMIN_EMAIL=admin@example.com
DIRECTUS_ADMIN_PASSWORD=your_secure_password_here

# -----------------------------------------------------------------------------
# Cloudflare R2 Storage (for Directus media)
# -----------------------------------------------------------------------------
STORAGE_LOCATIONS=r2
STORAGE_R2_DRIVER=s3
STORAGE_R2_KEY=your_r2_access_key_here
STORAGE_R2_SECRET=your_r2_secret_key_here
STORAGE_R2_BUCKET=chronos-media
STORAGE_R2_ENDPOINT=https://your_account_id.r2.cloudflarestorage.com
STORAGE_R2_REGION=auto
STORAGE_R2_PUBLIC_URL=https://media.yourdomain.com

# -----------------------------------------------------------------------------
# AWS S3 (if using S3 instead of R2)
# -----------------------------------------------------------------------------
S3_ACCESS_KEY_ID=your_aws_access_key_here
S3_SECRET_ACCESS_KEY=your_aws_secret_key_here
S3_BUCKET=project-chronos-media
S3_REGION=ca-central-1

# -----------------------------------------------------------------------------
# Application Settings
# -----------------------------------------------------------------------------
NODE_ENV=development
ENVIRONMENT=development
LOG_LEVEL=INFO
LOG_FORMAT=json

# Public URLs
NEXT_PUBLIC_SERVER_URL=http://localhost:3000
NEXT_PUBLIC_API_URL=http://localhost:3002

# Payload CMS
PAYLOAD_SECRET=your_payload_secret_here_32_chars

# Admin User Initialization (Used by create-admin-direct.mjs)
# ADMIN_EMAIL=admin@example.com
# ADMIN_PASSWORD=ChangeMe123!ur_admin_password_here

# -----------------------------------------------------------------------------
# External APIs - Data Sources
# -----------------------------------------------------------------------------
# FRED (Federal Reserve Economic Data)
# Get your free API key at: https://fred.stlouisfed.org/docs/api/api_key.html
FRED_API_KEY=your_fred_api_key_here

# -----------------------------------------------------------------------------
# Cloudflare Services
# -----------------------------------------------------------------------------
# Cloudflare API
CLOUDFLARE_API_TOKEN=your_cloudflare_api_token_here
CLOUDFLARE_ACCOUNT_ID=your_cloudflare_account_id_here

# Cloudflare Turnstile (CAPTCHA)
NEXT_PUBLIC_TURNSTILE_SITE_KEY=your_turnstile_site_key_here
TURNSTILE_SECRET_KEY=your_turnstile_secret_key_here

# Cloudflare Hyperdrive (optional - database connection pooling)
CLOUDFLARE_HYPERDRIVE_LOCAL_CONNECTION_STRING_DB=${DATABASE_URL}

# -----------------------------------------------------------------------------
# Vercel Deployment
# -----------------------------------------------------------------------------
VERCEL_OIDC_TOKEN=auto_generated_by_vercel
VERCEL_ENV=development
VERCEL_TARGET_ENV=development
VERCEL_URL=
VERCEL=1

# -----------------------------------------------------------------------------
# Nx Monorepo
# -----------------------------------------------------------------------------
NX_DAEMON=false
NX_CLOUD_ACCESS_TOKEN=your_nx_cloud_token_here

# Turbo Cache
TURBO_CACHE=remote:rw
TURBO_DOWNLOAD_LOCAL_ENABLED=true
TURBO_REMOTE_ONLY=true
TURBO_RUN_SUMMARY=true

# -----------------------------------------------------------------------------
# Monitoring & Error Tracking
# -----------------------------------------------------------------------------
# Sentry
SENTRY_AUTH_TOKEN=your_sentry_auth_token_here
SENTRY_ORG_SLUG=your_org_slug
SENTRY_ACCESS_TOKEN=your_sentry_access_token_here

# -----------------------------------------------------------------------------
# pgAdmin (Database Management UI)
# -----------------------------------------------------------------------------
PGADMIN_EMAIL=admin@example.com
PGADMIN_PASSWORD=your_pgadmin_password_here

# -----------------------------------------------------------------------------
# MCP Servers (for Claude Desktop integration)
# -----------------------------------------------------------------------------
# GitHub MCP
GITHUB_TOKEN=your_github_personal_access_token_here

# Postgres MCP
PGHOST=${DATABASE_HOST}
PGPORT=${DATABASE_PORT}
PGDATABASE=${DATABASE_NAME}

# Brave Search MCP
BRAVE_API_KEY=your_brave_api_key_here

# Directus CMS MCP
DIRECTUS_URL=https://admin.yourdomain.com
DIRECTUS_EMAIL=${DIRECTUS_ADMIN_EMAIL}
DIRECTUS_PASSWORD=${DIRECTUS_ADMIN_PASSWORD}

# TwentyCRM MCP
TWENTY_API_URL=https://crm.yourdomain.com
TWENTY_API_KEY=your_twenty_api_key_here

# Atlassian (Jira/Confluence) MCP
ATLASSIAN_INSTANCE_URL=https://yourcompany.atlassian.net
ATLASSIAN_EMAIL=your_email@example.com
ATLASSIAN_API_TOKEN=your_atlassian_api_token_here

# Resend Email MCP
RESEND_API_KEY=your_resend_api_key_here

# Google Workspace MCP (optional - setup OAuth credentials)
# GOOGLE_CLIENT_ID=your_google_client_id_here
# GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# -----------------------------------------------------------------------------
# Git Integration
# -----------------------------------------------------------------------------
VERCEL_GIT_COMMIT_AUTHOR_LOGIN=
VERCEL_GIT_COMMIT_AUTHOR_NAME=
VERCEL_GIT_COMMIT_MESSAGE=
VERCEL_GIT_COMMIT_REF=
VERCEL_GIT_COMMIT_SHA=
VERCEL_GIT_PREVIOUS_SHA=
VERCEL_GIT_PROVIDER=
VERCEL_GIT_PULL_REQUEST_ID=
VERCEL_GIT_REPO_ID=
VERCEL_GIT_REPO_OWNER=
VERCEL_GIT_REPO_SLUG=

# =============================================================================
# NOTES
# =============================================================================
# 1. Generate secure random values for secrets:
#    - DIRECTUS_KEY: openssl rand -hex 32
#    - DIRECTUS_SECRET: openssl rand -hex 32
#    - PAYLOAD_SECRET: openssl rand -hex 16
#
# 2. Never commit .env files to version control
#    - .env.local (local development)
#    - .env.production (production secrets)
#
# 3. For production deployment:
#    - Copy this file to .env.production
#    - Fill in actual production values
#    - Store securely (KeePassXC, 1Password, etc.)
#
# 4. MCP Server variables are only needed if using Claude Desktop integration
# =============================================================================
