name = "project-chronos"
compatibility_date = "2025-12-29"
# Required for Cloudflare Pages Functions
compatibility_flags = [ "nodejs_compat" ]

# Cloudflare Pages build output directory (OpenNext)
pages_build_output_dir = "apps/web/.open-next"

[vars]
# Public environment variables
NEXT_PUBLIC_SITE_URL = "https://automatonicai.com"
NEXT_PUBLIC_DIRECTUS_URL = "https://admin.automatonicai.com"

# TEMPORARILY DISABLED: Hyperdrive binding to allow GUI configuration
# Once Hyperdrive is properly configured in Cloudflare GUI, we can re-enable this
# [[hyperdrive]]
# binding = "DB"
# id = "004d95e1a3b94c01b4ba235c2b6167c3"
# localConnectionString only needed for local development/testing
# localConnectionString = "postgresql://user:pass@localhost:5432/chronos"

[[r2_buckets]]
binding = "MEDIA"
bucket_name = "chronos-media"
