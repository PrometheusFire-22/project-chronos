#!/bin/bash
#
# Quick alias for Confluence hierarchy organization
# Usage:
#   ./scripts/organize          # Organize hierarchy
#   ./scripts/organize --dry-run  # Preview changes
#

cd "$(dirname "$0")/.." || exit 1

source .venv/bin/activate
python3 scripts/ops/organize_confluence_hierarchy.py "$@"
